<axiom-guidelines>
=== .ai/actions rules ===

# Actions

- Prefer reusable Action classes in `app/Actions`.
- Name actions by what they do, with no `Action` suffix required by the call site.
- Keep a single public `handle()` method per action.
- Inject dependencies through the constructor using promoted properties.
- Create new actions with `php artisan make:action "{name}" --no-interaction`.
- Wrap multi-model writes in transactions.

=== .ai/data rules ===

# DTOs

- Prefer DTOs over loose arrays at application boundaries.
- Use readonly DTOs when the data should not change after construction.
- Keep DTOs explicit and fully typed.

=== .ai/quality rules ===

# Quality

- Push errors left with static analysis and strict CI rules.
- Prefer PHPStan, Rector, Pint, Oxlint, and Oxfmt at strict settings.
- Keep new code free from debug helpers and silent fallbacks.

=== .ai/php rules ===

# PHP

- Use `declare(strict_types=1);`.
- Prefer `final` classes by default.
- Prefer `readonly` objects when mutation is not required.
- Type every property, parameter, and return value.

=== .ai/laravel rules ===

# Laravel

- Keep controllers thin and move business logic into actions.
- Prefer Form Requests and DTOs over inline validation and loose arrays.
- Prefer Eloquent models and relationships over raw queries.
- Use named routes and `route()` for URL generation.
- Use `config()` outside config files, never `env()` directly.
- In Laravel 12, app setup belongs in `bootstrap/app.php` and app providers live in `bootstrap/providers.php`.
- Favor eager loading, strict model behavior, and immutable dates where appropriate.

=== .ai/testing rules ===

# Testing

- Every behavior change should be covered by a test or an updated existing test.
- Prefer Pest for tests and run the minimum relevant suite first.
- Use `php artisan test --compact` for targeted verification when available.
- Run `vendor/bin/pint --dirty` before finalizing code changes.

=== .ai/boost rules ===

# Laravel Boost

- Use Laravel Boost tools when they are available.
- Search Laravel documentation with the Boost docs search tool before changing framework-specific code.
- Use the Artisan command listing tool to verify command signatures before running unfamiliar commands.
- Use project-aware Laravel tooling instead of guessing framework behavior.

=== .ai/skills rules ===

# Skills

- If available, use `.ai/skills/actions.md` for action workflows.
- If available, use `.ai/skills/dto.md` for DTO workflows.
- If available, use `.ai/skills/enum.md` for enum workflows.
- If available, use `.ai/skills/crud.md` for CRUD workflows.
- If available, use `.ai/skills/quality.md` for verification and quality workflows.
</axiom-guidelines>
